<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.huang.sbp03.mapper.TeacherMapper"><!--命名域，绑定了UserMapper类-->

    <select id="teacherLogin" parameterType="Teacher" resultType="String">
        select pwd from mybatis01.teacher where name=#{name}
    </select>

</mapper>